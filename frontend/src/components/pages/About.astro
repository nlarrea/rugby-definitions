---
import Layout from '@/layouts/Layout.astro';
import { getI18N } from '@/i18n';

const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });
---

<Layout
	title={`${i18n.SEO.TITLE_ABOUT}`}
	description={i18n.SEO.DESCRIPTION_ABOUT}
>
	{i18n.SOON}
</Layout>
